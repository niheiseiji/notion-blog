# NiheiのNotion blog
## set up
1. Node.js 18 以上がインストールされていることを確認します。
2. 依存関係をインストールします。

```bash
npm install
```

3. 環境変数を `.env.local` に設定します。
   - `NOTION_TOKEN`
   - `NOTION_DATABASE_ID`
4. 開発サーバーを起動します。

```bash
npm run dev
```

5. ブラウザで `http://localhost:3000` を開き、動作を確認します。
